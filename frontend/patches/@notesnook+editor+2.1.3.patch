diff --git a/node_modules/@notesnook/editor/dist/index.d.mts b/node_modules/@notesnook/editor/dist/index.d.mts
index e4604f6..c9d2211 100644
--- a/node_modules/@notesnook/editor/dist/index.d.mts
+++ b/node_modules/@notesnook/editor/dist/index.d.mts
@@ -107,7 +107,7 @@ type ImageAlignmentOptions = {
 };
 type Attachment = FileAttachment | WebClipAttachment | ImageAttachment;
 
-type AttachmentType = "image" | "file" | "camera";
+type AttachmentType = "image" | "file" | "camera" | "draw" | "edit";
 interface AttachmentOptions {
     types: string[];
     HTMLAttributes: Record<string, unknown>;
@@ -849,7 +849,7 @@ interface TiptapStorage {
     timeFormat?: DateTimeOptions["timeFormat"];
     openLink?: (url: string) => void;
     downloadAttachment?: (attachment: Attachment) => void;
-    openAttachmentPicker?: (type: AttachmentType) => void;
+    openAttachmentPicker?: (type: AttachmentType, attachment?: Pick<Attachment, "hash" | "type">) => void;
     previewAttachment?: (attachment: Attachment) => void;
     copyToClipboard?: (text: string, html?: string) => void;
     createInternalLink?: (attributes?: LinkAttributes) => Promise<LinkAttributes | undefined>;
diff --git a/node_modules/@notesnook/editor/dist/index.d.ts b/node_modules/@notesnook/editor/dist/index.d.ts
index e4604f6..c9d2211 100644
--- a/node_modules/@notesnook/editor/dist/index.d.ts
+++ b/node_modules/@notesnook/editor/dist/index.d.ts
@@ -107,7 +107,7 @@ type ImageAlignmentOptions = {
 };
 type Attachment = FileAttachment | WebClipAttachment | ImageAttachment;
 
-type AttachmentType = "image" | "file" | "camera";
+type AttachmentType = "image" | "file" | "camera" | "draw" | "edit";
 interface AttachmentOptions {
     types: string[];
     HTMLAttributes: Record<string, unknown>;
@@ -849,7 +849,7 @@ interface TiptapStorage {
     timeFormat?: DateTimeOptions["timeFormat"];
     openLink?: (url: string) => void;
     downloadAttachment?: (attachment: Attachment) => void;
-    openAttachmentPicker?: (type: AttachmentType) => void;
+    openAttachmentPicker?: (type: AttachmentType, attachment?: Pick<Attachment, "hash" | "type">) => void;
     previewAttachment?: (attachment: Attachment) => void;
     copyToClipboard?: (text: string, html?: string) => void;
     createInternalLink?: (attributes?: LinkAttributes) => Promise<LinkAttributes | undefined>;
diff --git a/node_modules/@notesnook/editor/dist/index.mjs b/node_modules/@notesnook/editor/dist/index.mjs
index 643577e..993bf70 100644
--- a/node_modules/@notesnook/editor/dist/index.mjs
+++ b/node_modules/@notesnook/editor/dist/index.mjs
@@ -2902,17 +2902,25 @@ function HighlighterPlugin({
               }
             }
             if (changedBlocks.size > 0) {
-              const { tr } = view.state;
-              const changedNodes = findChildren2(
-                tr.doc,
-                (n) => n.type.name === name && changedBlocks.has(n.attrs.id)
-              );
-              changedNodes.forEach(({ node, pos }) => {
-                tr.setNodeMarkup(pos, node.type, node.attrs);
-              });
-              tr.setMeta("preventUpdate", true);
-              tr.setMeta("addToHistory", false);
-              view.dispatch(tr);
+              // Do not dispatch while ProseMirror is still updating plugin
+              // views. The language loader resolves asynchronously, but a
+              // cached module can resolve during the current update cycle and
+              // corrupt the decoration set (DecorationGroup.localsInner).
+              setTimeout(() => {
+                if (view.isDestroyed) return;
+                const { tr } = view.state;
+                const changedNodes = findChildren2(
+                  tr.doc,
+                  (n) => n.type.name === name && changedBlocks.has(n.attrs.id)
+                );
+                changedNodes.forEach(({ node, pos }) => {
+                  tr.setNodeMarkup(pos, node.type, node.attrs);
+                });
+                if (tr.steps.length === 0) return;
+                tr.setMeta("preventUpdate", true);
+                tr.setMeta("addToHistory", false);
+                view.dispatch(tr);
+              }, 0);
             }
           });
         }
@@ -4371,7 +4379,6 @@ function InsertBlock(props) {
       callout(editor),
       blockquote(editor),
       image(editor, isMobile),
-      attachment(editor),
       isMobile ? embedMobile(editor) : embedDesktop(editor),
       table(editor)
     ];
@@ -4502,15 +4509,23 @@ var image = (editor, isMobile) => ({
       {
         key: "camera",
         type: "button",
-        title: "Take a photo using camera",
+        title: "Take a photo with webcam",
         icon: Icons.camera,
-        isHidden: !isMobile,
         onClick: () => {
           var _a, _b;
           return (_b = (_a = editor.storage).openAttachmentPicker) == null ? void 0 : _b.call(_a, "camera");
         }
       },
-      isMobile ? uploadImageFromURLMobile(editor) : uploadImageFromURL(editor)
+      {
+        key: "draw",
+        type: "button",
+        title: "Draw a sketch",
+        icon: Icons.palette,
+        onClick: () => {
+          var _a, _b;
+          return (_b = (_a = editor.storage).openAttachmentPicker) == null ? void 0 : _b.call(_a, "draw");
+        }
+      },
     ]
   }
 });
@@ -4592,18 +4607,6 @@ var embedDesktop = (editor) => ({
     });
   }
 });
-var attachment = (editor) => ({
-  key: "attachment",
-  type: "button",
-  title: "Attachment",
-  icon: Icons.attachment,
-  isChecked: editor.isActive("attachment"),
-  onClick: () => {
-    var _a, _b;
-    return (_b = (_a = editor.storage).openAttachmentPicker) == null ? void 0 : _b.call(_a, "file");
-  },
-  modifier: "Mod-Shift-A"
-});
 var tasklist = (editor) => ({
   key: "tasklist",
   type: "button",
@@ -5728,7 +5731,7 @@ var DateTime = Extension.create({
   }
 });
 function shortcutInputRule(config2) {
-  const regex2 = new RegExp(`(^| )${config2.shortcut}(\\s)`, "i");
+  const regex2 = new RegExp(`(^| )${config2.shortcut}(\\s)$`, "i");
   return textInputRule({
     find: regex2,
     replace: (match) => {
@@ -7952,6 +7955,11 @@ var tools = {
     title: "Float image",
     conditional: true
   },
+  imageEdit: {
+    icon: "palette",
+    title: "Edit image",
+    conditional: true
+  },
   imageProperties: {
     icon: "more",
     title: "Image properties",
@@ -8075,20 +8083,18 @@ var defaultPresets = {
       "bold",
       "italic",
       "underline",
-      [
-        "strikethrough",
-        "code",
-        "subscript",
-        "superscript",
-        "highlight",
-        "textColor",
-        "math"
-      ]
+      "strikethrough",
+      "code",
+      "subscript",
+      "superscript",
+      "highlight",
+      "textColor",
+      "math"
     ],
     ["fontSize"],
     ["headings", "fontFamily"],
     ["checkList", "numberedList", "bulletList"],
-    ["addLink", "addInternalLink"],
+    ["addLink"],
     ["alignment", "textDirection"],
     ["clearformatting"]
   ],
@@ -8601,6 +8607,22 @@ function ImageFloat(props) {
     })
   );
 }
+function ImageEdit(props) {
+  const { editor } = props;
+  const image2 = findSelectedNode(editor, "image");
+  if (!image2 || !image2.attrs.hash) return null;
+  return /* @__PURE__ */ jsx34(
+    ToolButton,
+    __spreadProps(__spreadValues({}, props), {
+      toggled: false,
+      title: "Edit image",
+      onClick: () => {
+        var _a, _b;
+        return (_b = (_a = editor.storage).openAttachmentPicker) == null ? void 0 : _b.call(_a, "edit", { hash: image2.attrs.hash });
+      }
+    })
+  );
+}
 function ImageProperties2(props) {
   const { editor } = props;
   const [isOpen, setIsOpen] = useState12(false);
@@ -9328,6 +9350,7 @@ var tools2 = {
   imageAlignRight: ImageAlignRight,
   imageProperties: ImageProperties2,
   imageFloat: ImageFloat,
+  imageEdit: ImageEdit,
   embedAlignCenter: EmbedAlignCenter,
   embedAlignLeft: EmbedAlignLeft,
   embedAlignRight: EmbedAlignRight,
@@ -9606,13 +9629,6 @@ var AttachmentNode = Node8.create({
       }
     };
   },
-  addKeyboardShortcuts() {
-    return {
-      "Mod-Shift-A": () => {
-        var _a, _b;
-        return ((_b = (_a = this.editor.storage).openAttachmentPicker) == null ? void 0 : _b.call(_a, "file")) || true;
-      }
-    };
   }
   //   addInputRules() {
   //     return [
@@ -9626,7 +9642,7 @@ var AttachmentNode = Node8.create({
   //       }),
   //     ];
   //   },
-});
+);
 function getDataAttribute(name, def) {
   return {
     default: def,
@@ -10358,22 +10374,25 @@ function ImageComponent(props) {
   const isSVG = !!mime && mime.includes("/svg");
   useEffect14(() => {
     if (!inView) return;
-    if (src || !hash || bloburl) return;
+    if (src || !hash) return;
+    let cancelled = false;
     (function() {
       return __async(this, null, function* () {
         var _a, _b;
         const { hash: hash2 } = node.attrs;
-        if (hash2) {
-          const data = yield (_b = (_a = editor.storage).getAttachmentData) == null ? void 0 : _b.call(_a, {
-            type: "image",
-            hash: hash2
-          }).catch(() => null);
-          if (typeof data !== "string" || !data) return;
-          setBloburl(toBlobURL(data, "image", node.attrs.mime, hash2));
-        }
+        if (!hash2) return;
+        const data = yield (_b = (_a = editor.storage).getAttachmentData) == null ? void 0 : _b.call(_a, {
+          type: "image",
+          hash: hash2
+        }).catch(() => null);
+        if (cancelled || typeof data !== "string" || !data) return;
+        setBloburl(toBlobURL(data, "image", node.attrs.mime, hash2));
       });
     })();
-  }, [inView]);
+    return () => {
+      cancelled = true;
+    };
+  }, [inView, hash]);
   useEffect14(() => {
     const controller = controllerRef.current;
     return () => {
@@ -10429,7 +10448,7 @@ function ImageComponent(props) {
                       "imageAlignLeft",
                       "imageAlignCenter",
                       "imageAlignRight",
-                      "imageFloat",
+                      "imageEdit",
                       "imageProperties"
                     ],
                     sx: {
